# Швидкий старт

## Що потрібно зробити після завантаження

### 1. Скопіювати Assets

Вам потрібно скопіювати всі зображення з 7 оригінальних проектів:

**Найпростіший спосіб:**

1. Склонуйте всі 7 репозиторіїв в одну папку
2. Запустіть скрипт з файлу `ASSETS-GUIDE.md` (розділ "Швидке копіювання всього")

**Або вручну:**

Дивіться детальні інструкції в файлі `ASSETS-GUIDE.md`

### 2. Перевірити роботу

1. Відкрийте `index.html` у браузері
2. Оберіть будь-який прилад
3. Переконайтеся що:
   - Зображення плати відображається
   - Можна додавати компоненти
   - Розрахунки працюють

### 3. Налаштування (за потреби)

#### Зміна URL бази знань

Відредагуйте `data/devices-config.json`:
```json
{
  "backLink": "https://your-new-url.com/devices/..."
}
```

#### Додавання нового приладу

Дивіться розділ "Як додати новий прилад" в `README.md`

#### Додавання нових компонентів

Відредагуйте `data/components.json`

## Структура проекту (основне)

```
nova-calculators/
├── index.html              ← Головна сторінка (відкрийте це)
├── devices/                ← Сторінки калькуляторів
│   ├── nova-l-lte.html
│   └── ...
├── data/                   ← Конфігурації (можна редагувати)
│   ├── devices-config.json
│   └── components.json
├── assets/                 ← ⚠️ ПОТРІБНО ЗАПОВНИТИ!
│   ├── boards/            ← Зображення плат
│   ├── modules/           ← Зображення модулів
│   ├── kb/                ← Зображення клавіатур
│   └── ...
└── README.md              ← Повна документація
```

## Відмінності від оригіналів

### Що змінилося:

1. **Єдина кодова база** - один JavaScript файл замість 7
2. **JSON конфігурація** - всі дані приладів в окремих файлах
3. **Головна сторінка** - єдина точка входу з вибором приладу
4. **Стилізація** - в стилі сайту tiras.technology

### Що залишилося:

1. **Вся функціональність** - всі можливості оригінальних калькуляторів
2. **Розширювачі** - повна підтримка модулів розширення
3. **Універсальні слоти** - механіка роботи збережена
4. **Кастомні значення** - можливість додавати свої датчики/сирени

## Що далі?

1. ✅ Скопіюйте assets (див. ASSETS-GUIDE.md)
2. ✅ Перевірте роботу (відкрийте index.html)
3. ✅ Налаштуйте URL (якщо потрібно)
4. ✅ Розмістіть на хостингу

## Розміщення на хостингу

Проект повністю статичний, можна розмістити на:

- **GitHub Pages** (безкоштовно)
- **Netlify** (безкоштовно)
- **Vercel** (безкоштовно)
- Будь-який веб-сервер (Apache, Nginx)

Просто завантажте всю папку `nova-calculators` на хостинг.

## Підтримка

Якщо щось не працює:

1. Перевірте консоль браузера (F12 → Console)
2. Переконайтеся що всі assets скопійовані
3. Перевірте що шляхи до файлів правильні
4. Дивіться README.md для детальної інформації

## Ключові файли для редагування

- `data/devices-config.json` - налаштування приладів
- `data/components.json` - дані компонентів
- `styles/main.css` - стилі головної сторінки
- `styles/calculator.css` - стилі калькулятора

**Не потрібно редагувати:**
- `scripts/calculator.js` - універсальна логіка
- `scripts/home.js` - логіка головної сторінки
- HTML файли в `devices/` - генеруються автоматично

Успіхів! 🚀
